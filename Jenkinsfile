pipeline {
  agent any
  
  stages {
    stage('Checkout') {
      step {
        // Checkout from git
      }
    }
    
    stage('Install dependencies') {
      step {
        // Install pods
      }
    }
    
    stage('Build') {
      step {
        // run xcode build
      }
    }
    
    stage('Test') {
      step {
        // Run xcode tests
      }
    }
    
    stage('Archive') {
      step {
        // Run xcode archive
      }
    }
  }

}
