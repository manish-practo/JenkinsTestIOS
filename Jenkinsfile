pipeline {
  agent any
  
  stages {
    stage('Checkout') {
      steps {
        // Checkout from git
      }
    }
    
    stage('Install dependencies') {
      steps {
        // Install pods
      }
    }
    
    stage('Build') {
      steps {
        // run xcode build
      }
    }
    
    stage('Test') {
      steps {
        // Run xcode tests
      }
    }
    
    stage('Archive') {
      steps {
        // Run xcode archive
      }
    }
  }

}
